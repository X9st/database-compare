# 数据库自动化比对工具 - 前端开发文档

## 文档信息

| 属性 | 内容 |
|------|------|
| 文档版本 | V1.0 |
| 编写日期 | 2026-03-24 |
| 技术栈 | Electron + React 18 + Ant Design 5.x + TypeScript |

---

# 1. 技术架构

## 1.1 技术选型

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 桌面框架 | Electron | ^28.0.0 | 跨平台桌面应用开发 |
| 前端框架 | React | ^18.2.0 | UI组件化开发 |
| UI组件库 | Ant Design | ^5.x | 企业级组件库 |
| 开发语言 | TypeScript | ^5.0.0 | 类型安全 |
| 状态管理 | Zustand | ^4.4.0 | 轻量级状态管理 |
| 路由管理 | React Router | ^6.x | 页面路由 |
| HTTP客户端 | Axios | ^1.6.0 | API请求 |
| 构建工具 | Vite | ^5.0.0 | 快速开发构建 |
| 图表库 | ECharts | ^5.4.0 | 数据可视化 |
| 样式方案 | Less + CSS Modules | - | 样式隔离 |

## 1.2 架构设计图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            Electron Main Process                            │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐  ┌──────────────┐  │
│  │ Window Manager│  │ Backend Starter│  │  IPC Handler  │  │ Auto Updater │  │
│  └───────────────┘  └───────────────┘  └───────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↕ IPC
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Electron Renderer Process                          │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                           React Application                          │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌────────────┐  │    │
│  │  │    Pages    │  │  Components │  │   Stores    │  │  Services  │  │    │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └────────────┘  │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↕ HTTP API
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Python Backend (FastAPI)                           │
│                          http://localhost:18765                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 1.3 进程通信设计

| 通信类型 | 场景 | 方式 |
|---------|------|------|
| 主进程 ↔ 渲染进程 | 窗口操作、系统功能 | Electron IPC |
| 渲染进程 ↔ Python后端 | 业务数据交互 | HTTP REST API |
| 渲染进程 ← Python后端 | 比对进度推送 | WebSocket / SSE |

---

# 2. 项目结构

```
database-compare/
├── electron/                      # Electron主进程代码
│   ├── main.ts                    # 主进程入口
│   ├── preload.ts                 # 预加载脚本
│   ├── ipc/                       # IPC处理器
│   │   ├── index.ts
│   │   ├── window.ts              # 窗口相关IPC
│   │   └── system.ts              # 系统相关IPC
│   └── utils/
│       ├── backend.ts             # Python后端管理
│       └── store.ts               # 本地存储
│
├── src/                           # React渲染进程代码
│   ├── main.tsx                   # 应用入口
│   ├── App.tsx                    # 根组件
│   ├── vite-env.d.ts              # 类型声明
│   │
│   ├── assets/                    # 静态资源
│   │   ├── images/
│   │   └── styles/
│   │       ├── global.less        # 全局样式
│   │       ├── variables.less     # 样式变量
│   │       └── antd-override.less # Ant Design覆盖
│   │
│   ├── components/                # 通用组件
│   │   ├── common/                # 基础通用组件
│   │   │   ├── PageHeader/
│   │   │   ├── LoadingOverlay/
│   │   │   └── EmptyState/
│   │   ├── business/              # 业务通用组件
│   │   │   ├── DatabaseSelector/  # 数据库选择器
│   │   │   ├── TableSelector/     # 表选择器
│   │   │   ├── DiffViewer/        # 差异查看器
│   │   │   └── ProgressPanel/     # 进度面板
│   │   └── layout/                # 布局组件
│   │       ├── AppLayout/
│   │       ├── Sidebar/
│   │       └── HeaderBar/
│   │
│   ├── pages/                     # 页面组件
│   │   ├── DataSource/            # 数据源配置
│   │   │   ├── index.tsx
│   │   │   ├── DataSourceList.tsx
│   │   │   ├── DataSourceForm.tsx
│   │   │   └── ConnectionTest.tsx
│   │   ├── Compare/               # 比对任务
│   │   │   ├── index.tsx
│   │   │   ├── TableSelect.tsx
│   │   │   ├── CompareConfig.tsx
│   │   │   └── CompareProgress.tsx
│   │   ├── Result/                # 比对结果
│   │   │   ├── index.tsx
│   │   │   ├── ResultSummary.tsx
│   │   │   ├── StructureDiff.tsx
│   │   │   ├── DataDiff.tsx
│   │   │   └── ExportPanel.tsx
│   │   ├── History/               # 历史记录
│   │   │   ├── index.tsx
│   │   │   └── HistoryList.tsx
│   │   └── Settings/              # 系统设置
│   │       ├── index.tsx
│   │       ├── GeneralSettings.tsx
│   │       ├── IgnoreRules.tsx
│   │       └── TemplateManager.tsx
│   │
│   ├── stores/                    # 状态管理
│   │   ├── index.ts
│   │   ├── dataSourceStore.ts     # 数据源状态
│   │   ├── compareStore.ts        # 比对任务状态
│   │   ├── resultStore.ts         # 比对结果状态
│   │   └── settingsStore.ts       # 设置状态
│   │
│   ├── services/                  # API服务
│   │   ├── api.ts                 # Axios实例配置
│   │   ├── dataSourceApi.ts       # 数据源API
│   │   ├── compareApi.ts          # 比对API
│   │   ├── resultApi.ts           # 结果API
│   │   └── settingsApi.ts         # 设置API
│   │
│   ├── hooks/                     # 自定义Hooks
│   │   ├── useDataSource.ts
│   │   ├── useCompare.ts
│   │   ├── useWebSocket.ts
│   │   └── useExport.ts
│   │
│   ├── types/                     # 类型定义
│   │   ├── index.ts
│   │   ├── dataSource.ts
│   │   ├── compare.ts
│   │   ├── result.ts
│   │   └── api.ts
│   │
│   ├── utils/                     # 工具函数
│   │   ├── constants.ts           # 常量定义
│   │   ├── format.ts              # 格式化工具
│   │   ├── validate.ts            # 校验工具
│   │   └── storage.ts             # 本地存储工具
│   │
│   └── router/                    # 路由配置
│       └── index.tsx
│
├── public/                        # 公共资源
│   └── icons/
│
├── package.json
├── tsconfig.json
├── vite.config.ts
├── electron-builder.json          # 打包配置
└── .env                           # 环境变量
```

---

# 3. 页面设计

## 3.1 页面路由

| 路径 | 页面 | 说明 |
|------|------|------|
| `/` | 数据源配置 | 首页，管理数据库连接 |
| `/compare` | 比对任务 | 创建和执行比对任务 |
| `/result/:taskId` | 比对结果 | 查看比对结果详情 |
| `/history` | 历史记录 | 历史比对记录列表 |
| `/settings` | 系统设置 | 比对配置、忽略规则等 |

## 3.2 页面布局

```
┌────────────────────────────────────────────────────────────────────┐
│  ■ 数据库比对工具              — □ ✕  │  (标题栏 - 可拖拽)
├────────────────────────────────────────────────────────────────────┤
│ ┌──────────┐ ┌──────────────────────────────────────────────────┐ │
│ │          │ │                                                  │ │
│ │  数据源  │ │                                                  │ │
│ │          │ │                                                  │ │
│ │ ──────── │ │                                                  │ │
│ │          │ │                   主内容区                        │ │
│ │  比对任务 │ │                                                  │ │
│ │          │ │                                                  │ │
│ │ ──────── │ │                                                  │ │
│ │          │ │                                                  │ │
│ │  历史记录 │ │                                                  │ │
│ │          │ │                                                  │ │
│ │ ──────── │ │                                                  │ │
│ │          │ │                                                  │ │
│ │  系统设置 │ │                                                  │ │
│ │          │ │                                                  │ │
│ └──────────┘ └──────────────────────────────────────────────────┘ │
│   (侧边栏)                                                         │
└────────────────────────────────────────────────────────────────────┘
```

## 3.3 核心页面设计

### 3.3.1 数据源配置页

**功能模块：**

| 模块 | 功能点 |
|------|--------|
| 数据源列表 | 展示已保存数据源，支持分组、搜索、排序 |
| 新增/编辑表单 | 配置数据库连接信息 |
| 连接测试 | 验证数据库连通性 |
| 分组管理 | 数据源分组的增删改 |

**表单字段：**

```typescript
interface DataSourceForm {
  name: string;              // 数据源名称
  group: string;             // 所属分组
  dbType: DatabaseType;      // 数据库类型
  host: string;              // 主机地址
  port: number;              // 端口
  database: string;          // 数据库名
  schema?: string;           // Schema名（可选）
  username: string;          // 用户名
  password: string;          // 密码
  charset?: string;          // 字符集（默认UTF-8）
  timeout?: number;          // 超时时间（默认30秒）
}
```

### 3.3.2 比对任务页

**功能流程：**

```
选择数据源 → 选择比对表 → 配置比对选项 → 启动比对 → 实时进度
```

**步骤组件：**

| 步骤 | 组件 | 功能 |
|------|------|------|
| Step 1 | DataSourcePicker | 选择源库和目标库 |
| Step 2 | TableSelector | 选择比对表（全库/指定表/排除表） |
| Step 3 | CompareOptions | 配置比对模式、映射、忽略规则 |
| Step 4 | CompareExecution | 启动比对、展示实时进度 |

### 3.3.3 比对结果页

**Tab页设计：**

| Tab | 内容 |
|-----|------|
| 概览 | 比对汇总统计卡片、整体结论 |
| 结构差异 | 表结构差异列表，支持筛选 |
| 数据差异 | 数据行差异列表，支持筛选、定位 |
| 导出 | 报告导出配置和操作 |

**差异展示组件：**

```typescript
// 结构差异项
interface StructureDiffItem {
  tableName: string;
  diffType: 'table_missing' | 'table_extra' | 'column_diff' | 'index_diff' | 'constraint_diff';
  sourceValue: string;
  targetValue: string;
  fieldName?: string;
  diffDetail: string;
}

// 数据差异项
interface DataDiffItem {
  tableName: string;
  primaryKey: Record<string, any>;
  diffType: 'row_missing' | 'row_extra' | 'value_diff' | 'null_diff';
  sourceRow?: Record<string, any>;
  targetRow?: Record<string, any>;
  diffColumns: string[];
}
```

---

# 4. 组件设计

## 4.1 通用组件

### 4.1.1 DatabaseSelector 数据库选择器

```typescript
interface DatabaseSelectorProps {
  value?: string;                    // 选中的数据源ID
  onChange?: (id: string) => void;   // 选择回调
  placeholder?: string;              // 占位文本
  showTestButton?: boolean;          // 是否显示测试按钮
  disabled?: boolean;
}
```

### 4.1.2 TableSelector 表选择器

```typescript
interface TableSelectorProps {
  dataSourceId: string;              // 数据源ID
  mode: 'all' | 'include' | 'exclude'; // 选择模式
  selectedTables?: string[];         // 已选表
  onChange?: (tables: string[]) => void;
  loading?: boolean;
}
```

### 4.1.3 DiffViewer 差异查看器

```typescript
interface DiffViewerProps {
  sourceValue: any;                  // 源值
  targetValue: any;                  // 目标值
  type: 'text' | 'json' | 'table';   // 展示类型
  highlightDiff?: boolean;           // 高亮差异
}
```

### 4.1.4 ProgressPanel 进度面板

```typescript
interface ProgressPanelProps {
  taskId: string;                    // 任务ID
  onComplete?: (result: CompareResult) => void;
  onError?: (error: Error) => void;
}
```

## 4.2 业务组件

### 4.2.1 结构差异表格

| 列名 | 字段 | 宽度 | 说明 |
|------|------|------|------|
| 表名 | tableName | 150px | 支持筛选 |
| 差异类型 | diffType | 120px | Tag展示 |
| 字段名 | fieldName | 120px | - |
| 源库值 | sourceValue | - | 自适应 |
| 目标库值 | targetValue | - | 自适应 |

### 4.2.2 数据差异表格

| 列名 | 字段 | 宽度 | 说明 |
|------|------|------|------|
| 表名 | tableName | 150px | 支持筛选 |
| 主键 | primaryKey | 150px | JSON展示 |
| 差异类型 | diffType | 100px | Tag展示 |
| 差异字段 | diffColumns | - | 多个用逗号分隔 |
| 操作 | - | 100px | 查看详情 |

---

# 5. 状态管理

## 5.1 Store设计

### 5.1.1 dataSourceStore

```typescript
interface DataSourceState {
  // 状态
  dataSources: DataSource[];
  groups: DataSourceGroup[];
  loading: boolean;
  
  // 操作
  fetchDataSources: () => Promise<void>;
  addDataSource: (data: CreateDataSourceDto) => Promise<void>;
  updateDataSource: (id: string, data: UpdateDataSourceDto) => Promise<void>;
  deleteDataSource: (id: string) => Promise<void>;
  testConnection: (id: string) => Promise<TestResult>;
  
  // 分组操作
  fetchGroups: () => Promise<void>;
  addGroup: (name: string) => Promise<void>;
  deleteGroup: (id: string) => Promise<void>;
}
```

### 5.1.2 compareStore

```typescript
interface CompareState {
  // 当前任务配置
  currentTask: CompareTaskConfig | null;
  
  // 任务执行状态
  taskStatus: 'idle' | 'running' | 'paused' | 'completed' | 'error';
  progress: CompareProgress;
  
  // 操作
  setSourceDb: (id: string) => void;
  setTargetDb: (id: string) => void;
  setTables: (tables: TableSelection) => void;
  setCompareOptions: (options: CompareOptions) => void;
  
  startCompare: () => Promise<string>;  // 返回taskId
  pauseCompare: () => Promise<void>;
  resumeCompare: () => Promise<void>;
  stopCompare: () => Promise<void>;
}

interface CompareProgress {
  totalTables: number;
  completedTables: number;
  currentTable: string;
  currentPhase: 'structure' | 'data';
  percentage: number;
  startTime: number;
  elapsedTime: number;
  estimatedRemaining?: number;
}
```

### 5.1.3 resultStore

```typescript
interface ResultState {
  // 当前结果
  currentResult: CompareResult | null;
  
  // 差异筛选
  structureFilter: StructureFilter;
  dataFilter: DataFilter;
  
  // 操作
  fetchResult: (taskId: string) => Promise<void>;
  setStructureFilter: (filter: StructureFilter) => void;
  setDataFilter: (filter: DataFilter) => void;
  exportReport: (format: 'excel' | 'html' | 'txt') => Promise<void>;
}
```

---

# 6. API服务封装

## 6.1 Axios实例配置

```typescript
// src/services/api.ts
import axios from 'axios';
import { message } from 'antd';

const API_BASE_URL = 'http://localhost:18765/api/v1';

const api = axios.create({
  baseURL: API_BASE_URL,
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json',
  },
});

// 请求拦截器
api.interceptors.request.use(
  (config) => {
    // 添加请求时间戳（用于日志）
    config.metadata = { startTime: Date.now() };
    return config;
  },
  (error) => Promise.reject(error)
);

// 响应拦截器
api.interceptors.response.use(
  (response) => {
    return response.data;
  },
  (error) => {
    const errorMsg = error.response?.data?.message || '请求失败，请稍后重试';
    message.error(errorMsg);
    return Promise.reject(error);
  }
);

export default api;
```

## 6.2 API模块示例

```typescript
// src/services/dataSourceApi.ts
import api from './api';
import { DataSource, CreateDataSourceDto, TestConnectionResult } from '@/types';

export const dataSourceApi = {
  // 获取数据源列表
  getList: () => api.get<DataSource[]>('/datasources'),
  
  // 获取单个数据源
  getById: (id: string) => api.get<DataSource>(`/datasources/${id}`),
  
  // 创建数据源
  create: (data: CreateDataSourceDto) => api.post<DataSource>('/datasources', data),
  
  // 更新数据源
  update: (id: string, data: Partial<CreateDataSourceDto>) => 
    api.put<DataSource>(`/datasources/${id}`, data),
  
  // 删除数据源
  delete: (id: string) => api.delete(`/datasources/${id}`),
  
  // 测试连接
  testConnection: (id: string) => 
    api.post<TestConnectionResult>(`/datasources/${id}/test`),
  
  // 获取数据库表列表
  getTables: (id: string, schema?: string) => 
    api.get<string[]>(`/datasources/${id}/tables`, { params: { schema } }),
  
  // 获取表结构信息
  getTableSchema: (id: string, tableName: string) =>
    api.get(`/datasources/${id}/tables/${tableName}/schema`),
};
```

---

# 7. 类型定义

## 7.1 数据源类型

```typescript
// src/types/dataSource.ts

// 数据库类型枚举
export type DatabaseType = 
  | 'mysql' 
  | 'oracle' 
  | 'sqlserver' 
  | 'postgresql' 
  | 'dm'        // 达梦
  | 'inceptor';

// 数据源实体
export interface DataSource {
  id: string;
  name: string;
  groupId?: string;
  groupName?: string;
  dbType: DatabaseType;
  host: string;
  port: number;
  database: string;
  schema?: string;
  username: string;
  charset: string;
  timeout: number;
  createdAt: string;
  updatedAt: string;
}

// 数据源分组
export interface DataSourceGroup {
  id: string;
  name: string;
  count: number;
}

// 创建数据源DTO
export interface CreateDataSourceDto {
  name: string;
  groupId?: string;
  dbType: DatabaseType;
  host: string;
  port: number;
  database: string;
  schema?: string;
  username: string;
  password: string;
  charset?: string;
  timeout?: number;
}

// 连接测试结果
export interface TestConnectionResult {
  success: boolean;
  message: string;
  latency?: number;  // 延迟毫秒数
  version?: string;  // 数据库版本
}
```

## 7.2 比对任务类型

```typescript
// src/types/compare.ts

// 表选择模式
export type TableSelectionMode = 'all' | 'include' | 'exclude';

// 比对模式
export type CompareMode = 'full' | 'incremental';

// 表选择配置
export interface TableSelection {
  mode: TableSelectionMode;
  tables: string[];
}

// 表映射配置
export interface TableMapping {
  sourceTable: string;
  targetTable: string;
  columnMappings?: ColumnMapping[];
}

// 字段映射
export interface ColumnMapping {
  sourceColumn: string;
  targetColumn: string;
}

// 忽略规则
export interface IgnoreRule {
  id: string;
  name: string;
  type: 'column' | 'dataType' | 'diffType' | 'table';
  pattern: string;       // 支持通配符
  tables?: string[];     // 适用的表
  enabled: boolean;
}

// 比对选项
export interface CompareOptions {
  mode: CompareMode;
  
  // 增量比对配置
  incrementalConfig?: {
    timeColumn: string;
    startTime: string;
    endTime?: string;
  };
  
  // 结构比对选项
  structureOptions: {
    compareIndex: boolean;
    compareConstraint: boolean;
    compareComment: boolean;
  };
  
  // 数据比对选项
  dataOptions: {
    floatPrecision: number;      // 浮点精度（小数位数）
    ignoreCase: boolean;         // 忽略大小写
    trimWhitespace: boolean;     // 去除前后空格
    dateTimePrecision: 'second' | 'millisecond';
    skipLargeFields: boolean;    // 跳过大字段
    pageSize: number;            // 分页大小
  };
  
  // 表映射
  tableMappings: TableMapping[];
  
  // 忽略规则
  ignoreRules: string[];  // 规则ID列表
}

// 比对任务配置
export interface CompareTaskConfig {
  sourceId: string;
  targetId: string;
  tableSelection: TableSelection;
  options: CompareOptions;
}
```

## 7.3 比对结果类型

```typescript
// src/types/result.ts

// 结构差异类型
export type StructureDiffType = 
  | 'table_missing_in_target'   // 目标库缺表
  | 'table_extra_in_target'     // 目标库多表
  | 'column_missing'            // 缺少字段
  | 'column_extra'              // 多余字段
  | 'column_type_diff'          // 字段类型不同
  | 'column_length_diff'        // 字段长度不同
  | 'column_nullable_diff'      // 可空属性不同
  | 'column_default_diff'       // 默认值不同
  | 'index_diff'                // 索引不同
  | 'constraint_diff'           // 约束不同
  | 'comment_diff';             // 注释不同

// 数据差异类型
export type DataDiffType =
  | 'row_count_diff'            // 行数不同
  | 'row_missing_in_target'     // 目标库缺行
  | 'row_extra_in_target'       // 目标库多行
  | 'value_diff'                // 字段值不同
  | 'null_diff';                // 空值差异

// 结构差异项
export interface StructureDiffItem {
  id: string;
  tableName: string;
  diffType: StructureDiffType;
  fieldName?: string;
  sourceValue: string | null;
  targetValue: string | null;
  diffDetail: string;
}

// 数据差异项
export interface DataDiffItem {
  id: string;
  tableName: string;
  primaryKey: Record<string, any>;
  diffType: DataDiffType;
  diffColumns: string[];
  sourceValues?: Record<string, any>;
  targetValues?: Record<string, any>;
}

// 表比对结果
export interface TableCompareResult {
  tableName: string;
  structureMatch: boolean;
  dataMatch: boolean;
  sourceRowCount: number;
  targetRowCount: number;
  structureDiffs: StructureDiffItem[];
  dataDiffs: DataDiffItem[];
  compareTime: number;  // 耗时毫秒
}

// 比对结果汇总
export interface CompareResultSummary {
  totalTables: number;
  structureMatchTables: number;
  structureDiffTables: number;
  dataMatchTables: number;
  dataDiffTables: number;
  totalStructureDiffs: number;
  totalDataDiffs: number;
}

// 完整比对结果
export interface CompareResult {
  taskId: string;
  status: 'completed' | 'partial' | 'failed';
  sourceDb: DataSource;
  targetDb: DataSource;
  startTime: string;
  endTime: string;
  duration: number;
  summary: CompareResultSummary;
  tableResults: TableCompareResult[];
  errorMessage?: string;
}
```

---

# 8. Electron主进程

## 8.1 主进程入口

```typescript
// electron/main.ts
import { app, BrowserWindow, ipcMain } from 'electron';
import path from 'path';
import { startBackend, stopBackend } from './utils/backend';
import { setupIpcHandlers } from './ipc';

let mainWindow: BrowserWindow | null = null;

async function createWindow() {
  mainWindow = new BrowserWindow({
    width: 1400,
    height: 900,
    minWidth: 1200,
    minHeight: 700,
    frame: false,  // 无边框窗口
    webPreferences: {
      preload: path.join(__dirname, 'preload.js'),
      nodeIntegration: false,
      contextIsolation: true,
    },
  });

  // 开发环境加载Vite服务
  if (process.env.NODE_ENV === 'development') {
    await mainWindow.loadURL('http://localhost:5173');
    mainWindow.webContents.openDevTools();
  } else {
    await mainWindow.loadFile(path.join(__dirname, '../dist/index.html'));
  }
}

app.whenReady().then(async () => {
  // 启动Python后端
  await startBackend();
  
  // 设置IPC处理器
  setupIpcHandlers();
  
  // 创建窗口
  await createWindow();
});

app.on('window-all-closed', async () => {
  // 停止Python后端
  await stopBackend();
  
  if (process.platform !== 'darwin') {
    app.quit();
  }
});
```

## 8.2 Python后端管理

```typescript
// electron/utils/backend.ts
import { spawn, ChildProcess } from 'child_process';
import path from 'path';
import { app } from 'electron';

let backendProcess: ChildProcess | null = null;
const BACKEND_PORT = 18765;

export async function startBackend(): Promise<void> {
  const isDev = process.env.NODE_ENV === 'development';
  
  let pythonPath: string;
  let scriptPath: string;
  
  if (isDev) {
    // 开发环境
    pythonPath = 'python';
    scriptPath = path.join(process.cwd(), 'backend', 'main.py');
  } else {
    // 生产环境 - 使用打包的Python
    const resourcesPath = path.join(app.getPath('exe'), '..', 'resources');
    pythonPath = path.join(resourcesPath, 'python', 'python.exe');
    scriptPath = path.join(resourcesPath, 'backend', 'main.py');
  }

  return new Promise((resolve, reject) => {
    backendProcess = spawn(pythonPath, [scriptPath, '--port', String(BACKEND_PORT)]);
    
    backendProcess.stdout?.on('data', (data) => {
      console.log(`[Backend] ${data}`);
      // 检测到服务启动成功
      if (data.toString().includes('Uvicorn running')) {
        resolve();
      }
    });
    
    backendProcess.stderr?.on('data', (data) => {
      console.error(`[Backend Error] ${data}`);
    });
    
    // 超时处理
    setTimeout(() => {
      reject(new Error('Backend startup timeout'));
    }, 30000);
  });
}

export async function stopBackend(): Promise<void> {
  if (backendProcess) {
    backendProcess.kill();
    backendProcess = null;
  }
}
```

---

# 9. 开发规范

## 9.1 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 组件文件 | PascalCase | `DataSourceForm.tsx` |
| Hook文件 | camelCase | `useDataSource.ts` |
| 工具文件 | camelCase | `format.ts` |
| 类型文件 | camelCase | `dataSource.ts` |
| CSS类名 | kebab-case | `.data-source-form` |
| 常量 | UPPER_SNAKE_CASE | `API_BASE_URL` |
| 组件Props | PascalCase + Props | `DataSourceFormProps` |

## 9.2 组件规范

```typescript
// 组件模板
import React, { FC, memo } from 'react';
import styles from './index.module.less';

interface MyComponentProps {
  title: string;
  onConfirm?: () => void;
}

const MyComponent: FC<MyComponentProps> = memo(({ title, onConfirm }) => {
  return (
    <div className={styles.container}>
      <h3>{title}</h3>
      <button onClick={onConfirm}>确认</button>
    </div>
  );
});

MyComponent.displayName = 'MyComponent';

export default MyComponent;
```

## 9.3 Git提交规范

```
feat: 新增功能
fix: 修复bug
docs: 文档更新
style: 样式调整
refactor: 代码重构
perf: 性能优化
test: 测试相关
chore: 构建/工具相关
```

---

# 10. 构建与打包

## 10.1 开发命令

```bash
# 安装依赖
npm install

# 启动开发服务
npm run dev

# 仅启动前端
npm run dev:renderer

# 仅启动Electron
npm run dev:electron
```

## 10.2 打包配置

```json
// electron-builder.json
{
  "appId": "com.company.db-compare",
  "productName": "数据库比对工具",
  "directories": {
    "output": "release"
  },
  "files": [
    "dist/**/*",
    "electron/**/*"
  ],
  "extraResources": [
    {
      "from": "backend",
      "to": "backend"
    },
    {
      "from": "python-embedded",
      "to": "python"
    }
  ],
  "win": {
    "target": "portable",
    "icon": "public/icons/icon.ico"
  },
  "mac": {
    "target": "dmg",
    "icon": "public/icons/icon.icns"
  }
}
```

## 10.3 打包命令

```bash
# Windows打包
npm run build:win

# macOS打包
npm run build:mac

# 全平台打包
npm run build:all
```

---

# 11. 配置文件

## 11.1 package.json

```json
{
  "name": "database-compare",
  "version": "1.0.0",
  "description": "数据库自动化比对工具",
  "main": "electron/main.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:renderer\" \"npm run dev:electron\"",
    "dev:renderer": "vite",
    "dev:electron": "wait-on http://localhost:5173 && cross-env NODE_ENV=development electron .",
    "build": "tsc && vite build",
    "build:electron": "tsc -p tsconfig.electron.json",
    "build:win": "npm run build && npm run build:electron && electron-builder --win",
    "build:mac": "npm run build && npm run build:electron && electron-builder --mac",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.0",
    "antd": "^5.14.0",
    "@ant-design/icons": "^5.2.6",
    "zustand": "^4.5.0",
    "axios": "^1.6.7",
    "echarts": "^5.4.3",
    "echarts-for-react": "^3.0.2",
    "dayjs": "^1.11.10",
    "lodash-es": "^4.17.21"
  },
  "devDependencies": {
    "@types/react": "^18.2.55",
    "@types/react-dom": "^18.2.19",
    "@types/lodash-es": "^4.17.12",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.3.3",
    "vite": "^5.1.0",
    "less": "^4.2.0",
    "electron": "^28.2.0",
    "electron-builder": "^24.9.1",
    "concurrently": "^8.2.2",
    "wait-on": "^7.2.0",
    "cross-env": "^7.0.3"
  }
}
```

## 11.2 vite.config.ts

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  base: './',
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src'),
    },
  },
  css: {
    preprocessorOptions: {
      less: {
        javascriptEnabled: true,
        modifyVars: {
          // Ant Design 主题变量
          'primary-color': '#1890ff',
          'border-radius-base': '4px',
        },
      },
    },
  },
  server: {
    port: 5173,
    strictPort: true,
  },
  build: {
    outDir: 'dist',
    emptyOutDir: true,
  },
});
```

## 11.3 tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

## 11.4 tsconfig.electron.json

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "CommonJS",
    "moduleResolution": "node",
    "esModuleInterop": true,
    "strict": true,
    "outDir": "electron",
    "rootDir": "electron-src",
    "skipLibCheck": true
  },
  "include": ["electron-src/**/*"]
}
```

---

# 12. 核心实现代码

## 12.1 预加载脚本 preload.ts

```typescript
// electron/preload.ts
import { contextBridge, ipcRenderer } from 'electron';

// 暴露给渲染进程的API
contextBridge.exposeInMainWorld('electronAPI', {
  // 窗口控制
  minimizeWindow: () => ipcRenderer.invoke('window:minimize'),
  maximizeWindow: () => ipcRenderer.invoke('window:maximize'),
  closeWindow: () => ipcRenderer.invoke('window:close'),
  isMaximized: () => ipcRenderer.invoke('window:isMaximized'),
  
  // 窗口状态监听
  onMaximizeChange: (callback: (isMaximized: boolean) => void) => {
    ipcRenderer.on('window:maximize-change', (_, isMaximized) => callback(isMaximized));
  },
  
  // 文件操作
  saveFile: (options: { defaultPath: string; content: string }) => 
    ipcRenderer.invoke('file:save', options),
  
  // 系统信息
  getPlatform: () => process.platform,
});

// TypeScript类型声明
declare global {
  interface Window {
    electronAPI: {
      minimizeWindow: () => Promise<void>;
      maximizeWindow: () => Promise<void>;
      closeWindow: () => Promise<void>;
      isMaximized: () => Promise<boolean>;
      onMaximizeChange: (callback: (isMaximized: boolean) => void) => void;
      saveFile: (options: { defaultPath: string; content: string }) => Promise<string>;
      getPlatform: () => string;
    };
  }
}
```

## 12.2 IPC处理器

```typescript
// electron/ipc/window.ts
import { ipcMain, BrowserWindow } from 'electron';

export function setupWindowIpc(mainWindow: BrowserWindow) {
  ipcMain.handle('window:minimize', () => {
    mainWindow.minimize();
  });

  ipcMain.handle('window:maximize', () => {
    if (mainWindow.isMaximized()) {
      mainWindow.unmaximize();
    } else {
      mainWindow.maximize();
    }
  });

  ipcMain.handle('window:close', () => {
    mainWindow.close();
  });

  ipcMain.handle('window:isMaximized', () => {
    return mainWindow.isMaximized();
  });

  // 监听窗口状态变化
  mainWindow.on('maximize', () => {
    mainWindow.webContents.send('window:maximize-change', true);
  });

  mainWindow.on('unmaximize', () => {
    mainWindow.webContents.send('window:maximize-change', false);
  });
}
```

## 12.3 路由配置

```typescript
// src/router/index.tsx
import { createHashRouter, Navigate } from 'react-router-dom';
import AppLayout from '@/components/layout/AppLayout';
import DataSource from '@/pages/DataSource';
import Compare from '@/pages/Compare';
import Result from '@/pages/Result';
import History from '@/pages/History';
import Settings from '@/pages/Settings';

export const router = createHashRouter([
  {
    path: '/',
    element: <AppLayout />,
    children: [
      { index: true, element: <Navigate to="/datasource" replace /> },
      { path: 'datasource', element: <DataSource /> },
      { path: 'compare', element: <Compare /> },
      { path: 'result/:taskId', element: <Result /> },
      { path: 'history', element: <History /> },
      { path: 'settings', element: <Settings /> },
    ],
  },
]);
```

## 12.4 应用入口

```typescript
// src/main.tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import { RouterProvider } from 'react-router-dom';
import { ConfigProvider } from 'antd';
import zhCN from 'antd/locale/zh_CN';
import { router } from './router';
import './assets/styles/global.less';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ConfigProvider
      locale={zhCN}
      theme={{
        token: {
          colorPrimary: '#1890ff',
          borderRadius: 4,
        },
      }}
    >
      <RouterProvider router={router} />
    </ConfigProvider>
  </React.StrictMode>
);
```

## 12.5 布局组件 AppLayout

```typescript
// src/components/layout/AppLayout/index.tsx
import React, { useState, useEffect } from 'react';
import { Outlet, useNavigate, useLocation } from 'react-router-dom';
import { Layout, Menu } from 'antd';
import {
  DatabaseOutlined,
  SwapOutlined,
  HistoryOutlined,
  SettingOutlined,
} from '@ant-design/icons';
import TitleBar from '../TitleBar';
import styles from './index.module.less';

const { Sider, Content } = Layout;

const menuItems = [
  { key: '/datasource', icon: <DatabaseOutlined />, label: '数据源' },
  { key: '/compare', icon: <SwapOutlined />, label: '比对任务' },
  { key: '/history', icon: <HistoryOutlined />, label: '历史记录' },
  { key: '/settings', icon: <SettingOutlined />, label: '系统设置' },
];

const AppLayout: React.FC = () => {
  const navigate = useNavigate();
  const location = useLocation();
  const [collapsed, setCollapsed] = useState(false);

  const selectedKey = '/' + location.pathname.split('/')[1];

  return (
    <Layout className={styles.layout}>
      <TitleBar />
      <Layout className={styles.main}>
        <Sider
          collapsible
          collapsed={collapsed}
          onCollapse={setCollapsed}
          theme="light"
          width={200}
          className={styles.sider}
        >
          <Menu
            mode="inline"
            selectedKeys={[selectedKey]}
            items={menuItems}
            onClick={({ key }) => navigate(key)}
          />
        </Sider>
        <Content className={styles.content}>
          <Outlet />
        </Content>
      </Layout>
    </Layout>
  );
};

export default AppLayout;
```

## 12.6 无边框标题栏组件

```typescript
// src/components/layout/TitleBar/index.tsx
import React, { useState, useEffect } from 'react';
import { MinusOutlined, BorderOutlined, CloseOutlined, BlockOutlined } from '@ant-design/icons';
import styles from './index.module.less';

const TitleBar: React.FC = () => {
  const [isMaximized, setIsMaximized] = useState(false);

  useEffect(() => {
    // 初始化状态
    window.electronAPI?.isMaximized().then(setIsMaximized);
    // 监听变化
    window.electronAPI?.onMaximizeChange(setIsMaximized);
  }, []);

  const handleMinimize = () => window.electronAPI?.minimizeWindow();
  const handleMaximize = () => window.electronAPI?.maximizeWindow();
  const handleClose = () => window.electronAPI?.closeWindow();

  return (
    <div className={styles.titleBar}>
      <div className={styles.dragRegion}>
        <span className={styles.title}>数据库比对工具</span>
      </div>
      <div className={styles.controls}>
        <button className={styles.btn} onClick={handleMinimize}>
          <MinusOutlined />
        </button>
        <button className={styles.btn} onClick={handleMaximize}>
          {isMaximized ? <BlockOutlined /> : <BorderOutlined />}
        </button>
        <button className={`${styles.btn} ${styles.closeBtn}`} onClick={handleClose}>
          <CloseOutlined />
        </button>
      </div>
    </div>
  );
};

export default TitleBar;
```

```less
// src/components/layout/TitleBar/index.module.less
.titleBar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 32px;
  background: #fff;
  border-bottom: 1px solid #f0f0f0;
  -webkit-app-region: drag;  // 可拖拽
}

.dragRegion {
  flex: 1;
  padding-left: 12px;
}

.title {
  font-size: 14px;
  font-weight: 500;
}

.controls {
  display: flex;
  -webkit-app-region: no-drag;  // 按钮不可拖拽
}

.btn {
  width: 46px;
  height: 32px;
  border: none;
  background: transparent;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  
  &:hover {
    background: #e8e8e8;
  }
}

.closeBtn:hover {
  background: #e81123;
  color: #fff;
}
```

## 12.7 WebSocket Hook

```typescript
// src/hooks/useWebSocket.ts
import { useEffect, useRef, useCallback, useState } from 'react';

interface UseWebSocketOptions {
  onMessage?: (data: any) => void;
  onOpen?: () => void;
  onClose?: () => void;
  onError?: (error: Event) => void;
  reconnectInterval?: number;
  maxReconnectAttempts?: number;
}

export function useWebSocket(url: string | null, options: UseWebSocketOptions = {}) {
  const {
    onMessage,
    onOpen,
    onClose,
    onError,
    reconnectInterval = 3000,
    maxReconnectAttempts = 5,
  } = options;

  const wsRef = useRef<WebSocket | null>(null);
  const reconnectCountRef = useRef(0);
  const [isConnected, setIsConnected] = useState(false);

  const connect = useCallback(() => {
    if (!url) return;

    const ws = new WebSocket(url);

    ws.onopen = () => {
      setIsConnected(true);
      reconnectCountRef.current = 0;
      onOpen?.();
    };

    ws.onmessage = (event) => {
      try {
        const data = JSON.parse(event.data);
        onMessage?.(data);
      } catch {
        onMessage?.(event.data);
      }
    };

    ws.onclose = () => {
      setIsConnected(false);
      onClose?.();

      // 自动重连
      if (reconnectCountRef.current < maxReconnectAttempts) {
        reconnectCountRef.current++;
        setTimeout(connect, reconnectInterval);
      }
    };

    ws.onerror = (error) => {
      onError?.(error);
    };

    wsRef.current = ws;
  }, [url, onMessage, onOpen, onClose, onError, reconnectInterval, maxReconnectAttempts]);

  const disconnect = useCallback(() => {
    wsRef.current?.close();
    wsRef.current = null;
  }, []);

  const sendMessage = useCallback((data: any) => {
    if (wsRef.current?.readyState === WebSocket.OPEN) {
      wsRef.current.send(typeof data === 'string' ? data : JSON.stringify(data));
    }
  }, []);

  useEffect(() => {
    connect();
    return () => disconnect();
  }, [connect, disconnect]);

  return { isConnected, sendMessage, disconnect, reconnect: connect };
}
```

## 12.8 进度监听 Hook

```typescript
// src/hooks/useCompareProgress.ts
import { useState, useCallback } from 'react';
import { useWebSocket } from './useWebSocket';
import { CompareProgress } from '@/types';

const WS_BASE_URL = 'ws://localhost:18765';

interface ProgressMessage {
  type: 'progress' | 'completed' | 'failed';
  data: any;
}

export function useCompareProgress(taskId: string | null) {
  const [progress, setProgress] = useState<CompareProgress | null>(null);
  const [status, setStatus] = useState<'idle' | 'running' | 'completed' | 'failed'>('idle');
  const [error, setError] = useState<string | null>(null);

  const handleMessage = useCallback((message: ProgressMessage) => {
    switch (message.type) {
      case 'progress':
        setProgress(message.data.progress);
        setStatus('running');
        break;
      case 'completed':
        setStatus('completed');
        break;
      case 'failed':
        setStatus('failed');
        setError(message.data.error_message);
        break;
    }
  }, []);

  const wsUrl = taskId ? `${WS_BASE_URL}/ws/compare/tasks/${taskId}/progress` : null;

  const { isConnected, disconnect } = useWebSocket(wsUrl, {
    onMessage: handleMessage,
    onOpen: () => setStatus('running'),
  });

  return {
    progress,
    status,
    error,
    isConnected,
    stopListening: disconnect,
  };
}
```

---

# 13. 页面实现示例

## 13.1 数据源配置页

```typescript
// src/pages/DataSource/index.tsx
import React, { useEffect, useState } from 'react';
import { Button, Space, Input, message } from 'antd';
import { PlusOutlined, SearchOutlined } from '@ant-design/icons';
import { useDataSourceStore } from '@/stores/dataSourceStore';
import DataSourceList from './DataSourceList';
import DataSourceForm from './DataSourceForm';
import styles from './index.module.less';

const DataSource: React.FC = () => {
  const { dataSources, loading, fetchDataSources, deleteDataSource, testConnection } = 
    useDataSourceStore();
  const [formVisible, setFormVisible] = useState(false);
  const [editingId, setEditingId] = useState<string | null>(null);
  const [searchKeyword, setSearchKeyword] = useState('');

  useEffect(() => {
    fetchDataSources();
  }, [fetchDataSources]);

  const handleAdd = () => {
    setEditingId(null);
    setFormVisible(true);
  };

  const handleEdit = (id: string) => {
    setEditingId(id);
    setFormVisible(true);
  };

  const handleDelete = async (id: string) => {
    await deleteDataSource(id);
    message.success('删除成功');
  };

  const handleTest = async (id: string) => {
    const result = await testConnection(id);
    if (result.success) {
      message.success(`连接成功，延迟: ${result.latency}ms`);
    } else {
      message.error(`连接失败: ${result.message}`);
    }
  };

  const filteredDataSources = dataSources.filter(
    (ds) => ds.name.includes(searchKeyword) || ds.host.includes(searchKeyword)
  );

  return (
    <div className={styles.container}>
      <div className={styles.header}>
        <h2>数据源管理</h2>
        <Space>
          <Input
            placeholder="搜索数据源"
            prefix={<SearchOutlined />}
            value={searchKeyword}
            onChange={(e) => setSearchKeyword(e.target.value)}
            style={{ width: 200 }}
          />
          <Button type="primary" icon={<PlusOutlined />} onClick={handleAdd}>
            新增数据源
          </Button>
        </Space>
      </div>

      <DataSourceList
        dataSource={filteredDataSources}
        loading={loading}
        onEdit={handleEdit}
        onDelete={handleDelete}
        onTest={handleTest}
      />

      <DataSourceForm
        visible={formVisible}
        editingId={editingId}
        onClose={() => setFormVisible(false)}
        onSuccess={() => {
          setFormVisible(false);
          fetchDataSources();
        }}
      />
    </div>
  );
};

export default DataSource;
```

## 13.2 数据源表单组件

```typescript
// src/pages/DataSource/DataSourceForm.tsx
import React, { useEffect, useState } from 'react';
import { Modal, Form, Input, Select, InputNumber, Button, Space, message, Spin } from 'antd';
import { EyeInvisibleOutlined, EyeTwoTone } from '@ant-design/icons';
import { dataSourceApi } from '@/services/dataSourceApi';
import { DatabaseType, CreateDataSourceDto } from '@/types';

const DB_TYPE_OPTIONS = [
  { label: 'MySQL', value: 'mysql', defaultPort: 3306 },
  { label: 'Oracle', value: 'oracle', defaultPort: 1521 },
  { label: 'SQL Server', value: 'sqlserver', defaultPort: 1433 },
  { label: 'PostgreSQL', value: 'postgresql', defaultPort: 5432 },
  { label: '达梦 (DM)', value: 'dm', defaultPort: 5236 },
  { label: 'Inceptor', value: 'inceptor', defaultPort: 10000 },
];

interface Props {
  visible: boolean;
  editingId: string | null;
  onClose: () => void;
  onSuccess: () => void;
}

const DataSourceForm: React.FC<Props> = ({ visible, editingId, onClose, onSuccess }) => {
  const [form] = Form.useForm();
  const [loading, setLoading] = useState(false);
  const [testing, setTesting] = useState(false);

  const isEdit = !!editingId;

  useEffect(() => {
    if (visible && editingId) {
      // 加载编辑数据
      dataSourceApi.getById(editingId).then((res) => {
        form.setFieldsValue(res.data);
      });
    } else if (visible) {
      form.resetFields();
    }
  }, [visible, editingId, form]);

  const handleDbTypeChange = (dbType: DatabaseType) => {
    const option = DB_TYPE_OPTIONS.find((o) => o.value === dbType);
    if (option) {
      form.setFieldValue('port', option.defaultPort);
    }
  };

  const handleTestConnection = async () => {
    try {
      const values = await form.validateFields();
      setTesting(true);
      const result = await dataSourceApi.testConnectionDirect(values);
      if (result.data.success) {
        message.success(`连接成功！数据库版本: ${result.data.version}`);
      } else {
        message.error(`连接失败: ${result.data.message}`);
      }
    } catch (error) {
      // 表单校验失败
    } finally {
      setTesting(false);
    }
  };

  const handleSubmit = async () => {
    try {
      const values = await form.validateFields();
      setLoading(true);
      
      if (isEdit) {
        await dataSourceApi.update(editingId!, values);
        message.success('更新成功');
      } else {
        await dataSourceApi.create(values);
        message.success('创建成功');
      }
      
      onSuccess();
    } catch (error) {
      // 错误已由拦截器处理
    } finally {
      setLoading(false);
    }
  };

  return (
    <Modal
      title={isEdit ? '编辑数据源' : '新增数据源'}
      open={visible}
      onCancel={onClose}
      width={600}
      footer={
        <Space>
          <Button onClick={onClose}>取消</Button>
          <Button onClick={handleTestConnection} loading={testing}>
            测试连接
          </Button>
          <Button type="primary" onClick={handleSubmit} loading={loading}>
            {isEdit ? '更新' : '创建'}
          </Button>
        </Space>
      }
    >
      <Form form={form} layout="vertical" initialValues={{ charset: 'UTF-8', timeout: 30 }}>
        <Form.Item
          name="name"
          label="数据源名称"
          rules={[{ required: true, message: '请输入数据源名称' }]}
        >
          <Input placeholder="例如：生产环境-MySQL" />
        </Form.Item>

        <Form.Item
          name="dbType"
          label="数据库类型"
          rules={[{ required: true, message: '请选择数据库类型' }]}
        >
          <Select
            options={DB_TYPE_OPTIONS}
            onChange={handleDbTypeChange}
            placeholder="请选择数据库类型"
          />
        </Form.Item>

        <Space style={{ display: 'flex' }} align="start">
          <Form.Item
            name="host"
            label="主机地址"
            rules={[{ required: true, message: '请输入主机地址' }]}
            style={{ flex: 1 }}
          >
            <Input placeholder="192.168.1.100 或 db.example.com" />
          </Form.Item>
          <Form.Item
            name="port"
            label="端口"
            rules={[{ required: true, message: '请输入端口' }]}
            style={{ width: 120 }}
          >
            <InputNumber min={1} max={65535} style={{ width: '100%' }} />
          </Form.Item>
        </Space>

        <Form.Item
          name="database"
          label="数据库名"
          rules={[{ required: true, message: '请输入数据库名' }]}
        >
          <Input placeholder="请输入数据库名称" />
        </Form.Item>

        <Form.Item name="schema" label="Schema（可选）">
          <Input placeholder="Oracle/PostgreSQL需要填写" />
        </Form.Item>

        <Space style={{ display: 'flex' }} align="start">
          <Form.Item
            name="username"
            label="用户名"
            rules={[{ required: true, message: '请输入用户名' }]}
            style={{ flex: 1 }}
          >
            <Input placeholder="请输入用户名" />
          </Form.Item>
          <Form.Item
            name="password"
            label="密码"
            rules={[{ required: !isEdit, message: '请输入密码' }]}
            style={{ flex: 1 }}
          >
            <Input.Password
              placeholder={isEdit ? '留空表示不修改' : '请输入密码'}
              iconRender={(visible) => (visible ? <EyeTwoTone /> : <EyeInvisibleOutlined />)}
            />
          </Form.Item>
        </Space>

        <Space style={{ display: 'flex' }} align="start">
          <Form.Item name="charset" label="字符集" style={{ width: 150 }}>
            <Select
              options={[
                { label: 'UTF-8', value: 'UTF-8' },
                { label: 'GBK', value: 'GBK' },
                { label: 'GB2312', value: 'GB2312' },
              ]}
            />
          </Form.Item>
          <Form.Item name="timeout" label="超时时间(秒)" style={{ width: 150 }}>
            <InputNumber min={5} max={300} style={{ width: '100%' }} />
          </Form.Item>
        </Space>
      </Form>
    </Modal>
  );
};

export default DataSourceForm;
```

## 13.3 Zustand Store 实现

```typescript
// src/stores/dataSourceStore.ts
import { create } from 'zustand';
import { dataSourceApi } from '@/services/dataSourceApi';
import { DataSource, DataSourceGroup, CreateDataSourceDto, TestConnectionResult } from '@/types';

interface DataSourceState {
  dataSources: DataSource[];
  groups: DataSourceGroup[];
  loading: boolean;
  
  fetchDataSources: () => Promise<void>;
  addDataSource: (data: CreateDataSourceDto) => Promise<void>;
  updateDataSource: (id: string, data: Partial<CreateDataSourceDto>) => Promise<void>;
  deleteDataSource: (id: string) => Promise<void>;
  testConnection: (id: string) => Promise<TestConnectionResult>;
  
  fetchGroups: () => Promise<void>;
  addGroup: (name: string) => Promise<void>;
  deleteGroup: (id: string) => Promise<void>;
}

export const useDataSourceStore = create<DataSourceState>((set, get) => ({
  dataSources: [],
  groups: [],
  loading: false,

  fetchDataSources: async () => {
    set({ loading: true });
    try {
      const response = await dataSourceApi.getList();
      set({ dataSources: response.data });
    } finally {
      set({ loading: false });
    }
  },

  addDataSource: async (data) => {
    await dataSourceApi.create(data);
    await get().fetchDataSources();
  },

  updateDataSource: async (id, data) => {
    await dataSourceApi.update(id, data);
    await get().fetchDataSources();
  },

  deleteDataSource: async (id) => {
    await dataSourceApi.delete(id);
    set((state) => ({
      dataSources: state.dataSources.filter((ds) => ds.id !== id),
    }));
  },

  testConnection: async (id) => {
    const response = await dataSourceApi.testConnection(id);
    return response.data;
  },

  fetchGroups: async () => {
    // 实现分组获取
  },

  addGroup: async (name) => {
    // 实现分组添加
  },

  deleteGroup: async (id) => {
    // 实现分组删除
  },
}));
```
